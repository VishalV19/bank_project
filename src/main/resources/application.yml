spring:
  application:
    name: accounts

  datasource:
    url: jdbc:mysql://localhost:3306/fintech_accounts
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
